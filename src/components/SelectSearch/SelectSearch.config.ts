export type SelectSearchProps = {
    onChange?: (e: any) => void,
    options: {}[],
    onInputChange?: (e: any) => void,
    isLoading?: boolean,
    isSearchable? : boolean,
    placeholder?: string;
}